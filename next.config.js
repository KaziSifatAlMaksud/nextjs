/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: ["kazisifat.lgorithmbd.com"],
        unoptimized: true,
    },
    output: "export",
}

module.exports = nextConfig
